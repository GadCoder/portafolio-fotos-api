# Use root/example as user/password credentials
version: '3.1'

services:

  api:
    build: .
    container_name: api
    environment:
      PORT: 8000
    ports:
      - '8000:8000'
    restart: "no"


